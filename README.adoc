= Stubborn Contract Samples
:toc:
:toc-placement!:

Samples for https://github.com/stubborn-sh/stubborn-contract[Stubborn Contract] — the official continuation of Spring Cloud Contract.

toc::[]

== Samples

[cols="1,3"]
|===
|Sample |Description

|link:sample-http[sample-http]
|Basic REST/HTTP contracts — producer (Maven + Gradle) and consumer sides

|link:sample-messaging[sample-messaging]
|Messaging contracts via Spring Cloud Stream (RabbitMQ)

|link:sample-kafka[sample-kafka]
|Messaging contracts via Apache Kafka

|link:sample-stream[sample-stream]
|Spring Cloud Stream with explicit channel configuration

|link:sample-jms[sample-jms]
|JMS messaging contracts via Apache Artemis

|link:sample-rest-assured[sample-rest-assured]
|REST-Assured mode — explicit HTTP calls in generated tests

|link:sample-webflux[sample-webflux]
|Reactive WebFlux contracts using WebTestClient

|link:sample-security[sample-security]
|Secured endpoints — Basic Auth producer and consumer

|link:sample-restdocs[sample-restdocs]
|Spring REST Docs integration — generating WireMock stubs from documentation tests

|link:sample-wiremock[sample-wiremock]
|Standalone WireMock usage without the Stubborn Contract plugin

|link:sample-stubs-per-consumer[sample-stubs-per-consumer]
|Stubs-per-consumer pattern — each consumer gets tailored stubs

|link:sample-graphql[sample-graphql]
|GraphQL contracts using HTTP with `metadata(verifier.tool=graphql)`

|link:sample-grpc[sample-grpc]
|gRPC contracts with Protobuf and Spring Boot 4.1 native gRPC support

|link:sample-compatibility[sample-compatibility]
|SCC ↔ Stubborn interoperability: SCC stubs consumed by Stubborn runner and vice versa

|===

== Building

Each sample supports both Maven and Gradle.

[source,bash]
----
# Maven: Build producer (generates and installs stubs)
cd sample-http/producer
./mvnw clean install

# Maven: Build consumer (downloads stubs and runs contract tests)
cd ../consumer
./mvnw test

# Gradle: Build producer
cd sample-http/producer
./gradlew clean build

# Gradle: Build consumer
cd ../consumer
./gradlew clean build
----

== Requirements

* Java 17+
* Spring Boot 4.1.x
* Maven 3.9+ or Gradle 8.13+

== Dependency setup

Every sample imports a single BOM — no individual library versions required:

[source,xml]
----
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>sh.stubborn</groupId>
            <artifactId>stubborn-dependencies</artifactId>
            <version>0.1.0-SNAPSHOT</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>
----

The BOM manages compatible versions of `spring-cloud-stream` (5.0.2),
`rest-assured` (6.0.0), `spring-boot-restclient`, and all `sh.stubborn` artifacts.
Add the Maven Central snapshots repository to resolve the snapshot:

[source,xml]
----
<repositories>
    <repository>
        <id>central-snapshots</id>
        <url>https://central.sonatype.com/repository/maven-snapshots/</url>
        <snapshots><enabled>true</enabled></snapshots>
        <releases><enabled>false</enabled></releases>
    </repository>
</repositories>
----

== License

Apache 2.0
