pluginManagement {
    repositories {
        maven {
            url "https://central.sonatype.com/repository/maven-snapshots/"
            mavenContent { snapshotsOnly() }
        }
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = 'sample-wiremock'
